import 'package:flutter/material.dart';

class AppColors {
  static const Color backgroundColor = Color(
    0xFF20323E,
  ); // Dunkler, augenfreundlicher Hintergrund
  static const Color textColor = Color(
    0xFFFDFFFD,
  ); // Für allgemeinen Text, hohe Lesbarkeit
  static const Color primaryColor = Color(
    0xFF00B7F7,
  ); // Hauptaktionen wie Buttons, Call-to-Action
  static const Color secondaryColor = Color(
    0xFF7AD9F5,
  ); // Unterstützende Aktionen wie Filter, Auswahlfelder
  static const Color disabledPassive = Color(
    0xFF5A6B75,
  ); // Für deaktivierte Buttons, inaktive Felder
  static const Color accentColor = Color(
    0xFFCBA6F7,
  ); // Für visuelle Highlights, Icons
  static const Color errorColor = Color(0xFFF28B82); // Für Fehlermeldungen
  static const Color successColor = Color(0xFF7ED7A5); // Feedback bei Erfolg
  static const Color infoColor = Color(
    0xFFFFCB8E,
  ); // Hinweise, Tipps oder neutrale Benachrichtigungen
}
